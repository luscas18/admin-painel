'use strict';

/**
 *  vaga-emprego controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vaga-emprego.vaga-emprego');
